module.exports = context => `
<div>
  <section class="impression background-headlight">
    <div class="mid pad">
      ${context.website['Home / Quotes / leobard']}
    </div>
  </section>

  <section class="impression background-park">
    <div class="mid pad">
      ${context.website['Home / Quotes / knizz']}
    </div>
  </section>

  <section class="impression background-nuclear">
    <div class="mid pad">
      ${context.website['Home / Quotes / Soziomorph']}
    </div>
  </section>

  <section class="impression background-chaser">
    <div class="mid pad">
      ${context.website['Home / Quotes / ovos']}
    </div>
  </section>
</div>
`;
